#ifndef MICTYPE_H_INCLUDED
#define MICTYPE_H_INCLUDED
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int is_alpha (int );
int is_digit (int );
int to_upper (int );
int to_lower (int );


#endif // MICTYPE_H_INCLUDED
